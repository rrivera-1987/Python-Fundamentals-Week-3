my_tuple = (1, 2, 3, 4, 5)
print(my_tuple[0] + my_tuple[1]) #You can add numbers insiden the same tuple
print(my_tuple[-1] + my_tuple[-2]) #We can also add by going backwards with the last numbers.
# my_tuple[3] = 1 you cannot reassing a new value to an already existing index, because tuples are not mutable.